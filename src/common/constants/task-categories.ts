/**
 * Human-readable names for task categories, used wherever a category is shown
 * to a person rather than matched in code — the task-types picker and the
 * emails that announce a task.
 */
export const TASK_CATEGORY_LABELS: Record<string, string> = {
  LIKE_SHARE_SAVE_REPOST: 'Engagement (Likes, Repost, Retweet)',
  COMMENT_POST: 'Comments',
  MAKE_POST: 'Create Post',
  FOLLOW_ACCOUNT: 'Follow',
};
